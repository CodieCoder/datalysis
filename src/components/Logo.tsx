import Icon from "./Icon";

const Logo = () => {
  return (
    <span
      style={{
        fontSize: "1.3rem",
        fontWeight: 600,
      }}
    >
      <Icon icon={"Chart"} /> datalysis
    </span>
  );
};

export default Logo;
